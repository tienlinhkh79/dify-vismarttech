/**
 * Default product name shown in the console UI when server-side branding is disabled.
 * Does not affect API identifiers, URLs, or package names.
 */
export const APP_DISPLAY_NAME = 'Vismarttech' as const
